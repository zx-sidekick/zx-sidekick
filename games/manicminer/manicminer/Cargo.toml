[package]
name = "manicminer"
description = "Manic Miner's facts, its rules for the ZX Sidekick machine, and the font it prints with when there is no ROM"
# Manic Miner's version, the same in its crates (manicminer, the app and
# manicminer-check); its release is tagged manicminer-v<this>.
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
sidekick.workspace = true
zx-core.workspace = true
zx-spectrum.workspace = true

[lints]
workspace = true
