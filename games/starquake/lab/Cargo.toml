[package]
name = "starquake-lab"
description = "Tools for studying the game on the player's own tape: pictures, probes and searches. Not checks; never run in CI."
version = "0.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Already built for the app's zip support; here it compresses the pictures.
flate2.workspace = true
starquake.workspace = true
zx-core.workspace = true
zx-spectrum.workspace = true

[lints]
workspace = true
