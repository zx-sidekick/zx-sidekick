[package]
name = "sk-check"
description = "Local checks of ZX Sidekick against the player's own copy of the game (and, for one, a Spectrum ROM). Never run in CI."
version = "0.3.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
sidekick.workspace = true
zx-core.workspace = true
zx-spectrum.workspace = true

[lints]
workspace = true
